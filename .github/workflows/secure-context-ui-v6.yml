name: Secure Context UI V6

on:
  push:
    branches:
      - feature/game-detail-download-v2

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Apply secure-context UI
        run: python3 tools/secure-context-ui-v6.py

      - name: Validate focused checks
        run: |
          npm run lint
          npm run typecheck
          npm run check:architecture
          npm run check:data
          npm run check:routes
          npm run check:css-modules
          npm run security:scan -- --skip-npm-audit

      - name: Commit fix and remove temporary automation
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm tools/secure-context-ui-v6.py .github/workflows/secure-context-ui-v6.yml
          git add src/features/game-finder/types.ts src/features/game-finder/browser-detection.ts src/features/game-finder/GameFinderClient.tsx
          git commit -m "fix: avisar cuando la detección LAN está limitada"
          git push origin HEAD:feature/game-detail-download-v2
